<idea-plugin>
    <id>ch.erzberger.sharpbasic</id>
    <name>Sharp PC-1500 BASIC</name>
    <vendor email="contact@erzberger.ch" url="https://github.com/erzberger/SharpBasicPlugin">Erzberger</vendor>

    <description><![CDATA[
    <p>Language support for Sharp PC-1500/PC-1600 BASIC, vintage programming languages from the 1980s Sharp pocket computers.</p>
    <p>Features:</p>
    <ul>
        <li>Syntax highlighting for Sharp BASIC keywords, strings, numbers, and comments</li>
        <li>Code completion with full and abbreviated keyword forms</li>
        <li>Support for PC-1500 core commands (97 keywords)</li>
        <li>Support for CE-150 graphics/printer extension (14 keywords)</li>
        <li>Support for CE-158 communications extension (11 keywords)</li>
        <li>Support for PC-1600 keywords</li>
        <li>File type recognition for .bas and .pc1500 files</li>
    </ul>
    ]]></description>

    <change-notes><![CDATA[
    <h3>0.2.0</h3>
    <ul>
        <li>Major parser and lexer enhancements</li>
        <li>Keyword abbreviations now require a dot suffix (e.g. <code>P.</code> for <code>PRINT</code>)</li>
        <li>Fixed: <code>IF</code> conditions with <code>AND</code> and <code>OR</code> operators now parse correctly (e.g. <code>IF &7F AND T GOTO 10</code>)</li>
        <li>Fixed: hex numbers followed directly by keywords (no spaces) no longer swallow the first letter of the keyword</li>
        <li><code>#</code> is now recognised as a source-only comment at the start of a line, alongside <code>//</code></li>
    </ul>
    ]]></change-notes>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.lang</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- File Type -->
        <fileType
            name="Sharp BASIC"
            implementationClass="ch.erzberger.sharpbasic.SharpBasicFileType"
            fieldName="INSTANCE"
            language="SharpBasic"
            extensions="bas;pc1500"/>

        <!-- Parser -->
        <lang.parserDefinition
            language="SharpBasic"
            implementationClass="ch.erzberger.sharpbasic.parser.SharpBasicParserDefinition"/>

        <!-- Syntax Highlighter -->
        <lang.syntaxHighlighterFactory
            language="SharpBasic"
            implementationClass="ch.erzberger.sharpbasic.syntax.SharpBasicSyntaxHighlighterFactory"/>

        <!-- Code Completion -->
        <completion.contributor
            language="SharpBasic"
            implementationClass="ch.erzberger.sharpbasic.completion.SharpBasicCompletionContributor"/>

        <!-- Code Formatting -->
        <lang.formattingModelBuilder
            language="SharpBasic"
            implementationClass="ch.erzberger.sharpbasic.formatter.SharpBasicFormattingModelBuilder"/>
    </extensions>

    <actions>
        <!-- Reformat as PC-1500 BASIC action -->
        <action id="SharpBasic.ReformatAsPC1500"
                class="ch.erzberger.sharpbasic.formatter.ReformatAsPC1500Action"
                text="Reformat as PC-1500 BASIC"
                description="Reformat code to match Sharp PC-1500 canonical output format">
            <add-to-group group-id="CodeMenu" anchor="last"/>
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl alt shift F"/>
        </action>

        <!-- Reformat as Compact PC-1500 BASIC action -->
        <action id="SharpBasic.ReformatAsCompactPC1500"
                class="ch.erzberger.sharpbasic.formatter.ReformatAsCompactPC1500Action"
                text="Reformat as Compact PC-1500 BASIC"
                description="Compact code by removing spaces, comments, and using abbreviations">
            <add-to-group group-id="CodeMenu" anchor="last"/>
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl alt shift C"/>
        </action>

        <!-- Reformat as PC-1500 BASIC (Strip Comments) action -->
        <action id="SharpBasic.ReformatAsStrippedPC1500"
                class="ch.erzberger.sharpbasic.formatter.ReformatAsStrippedPC1500Action"
                text="Reformat as PC-1500 BASIC (Strip Comments)"
                description="Reformat to PC-1500 canonical format and remove // source comments (device-ready output)">
            <add-to-group group-id="CodeMenu" anchor="last"/>
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl alt shift D"/>
        </action>

        <!-- Reformat as Nice PC-1500 BASIC action -->
        <action id="SharpBasic.ReformatAsNicePC1500"
                class="ch.erzberger.sharpbasic.formatter.ReformatAsNicePC1500Action"
                text="Reformat as Nice PC-1500 BASIC"
                description="Reformat code into human-readable form with spaces around operators and keywords">
            <add-to-group group-id="CodeMenu" anchor="last"/>
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl alt shift N"/>
        </action>

        <!-- Renumber BASIC action -->
        <action id="SharpBasic.ReformatAsRenumbered"
                class="ch.erzberger.sharpbasic.formatter.ReformatAsRenumberedAction"
                text="Renumber BASIC"
                description="Renumber all BASIC line numbers to a canonical sequence (10, 20, 30, …) and remap all GOTO/GOSUB targets">
            <add-to-group group-id="CodeMenu" anchor="last"/>
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
            <keyboard-shortcut keymap="$default" first-keystroke="ctrl alt shift R"/>
        </action>
    </actions>
</idea-plugin>
